# 报告目录说明

`reports/` 分成两层：

- `examples/`：随仓库提交的正式样例报告，用于 README 和长期文档引用。
- `platform/`：平台运行时导出的报告、索引和注册表，默认不纳入版本控制。

## 维护原则

- `examples/` 只保留少量高代表性的报告，避免仓库因为批量回测结果持续膨胀。
- `platform/` 是运行产物，不作为长期文档事实来源。
- 如果需要引用新的样例报告，应同步更新 README 和相关文档。
