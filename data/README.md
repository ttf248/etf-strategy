# 数据目录说明

`data/` 只保留两类内容：

- `processed/`：历史兼容目录，当前只保留空目录占位，不再作为标准数据入口。
- 当前文档本身：用于说明目录边界，不承载任何正式数据。

## 提交边界

- 可以提交：目录说明、占位文件和明确约定的非数据元信息。
- 不应提交：历史行情、参考快照、一次性实验数据、临时导出 CSV。

所有正式行情数据都应通过 PostgreSQL 长期保存；本地 `data/processed/` 仅为兼容旧流程保留的空目录占位，不应承载长期或正式数据。
