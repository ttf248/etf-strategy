# 前端体验审查

本文记录面向“小白用户的回测平台”的前端页面审查结论。目标是把平台从运维控制台逐步调整为开箱即用的回测实验室。

## 总体判断

- 当前功能链路基本完整：数据准备、模板、提交回测、任务状态、报告列表和报告详情都有对应页面。
- 主要问题是信息架构偏运维：数据库、Worker、Scheduler、日志和模板管理过早暴露，用户还没完成第一次回测就需要理解平台内部概念。
- 第一轮调整已完成：导航改为“新手首页、创建回测、查看报告、数据准备、策略模板”，系统状态降级为维护入口；首页和回测页改为新手流程。

## 页面审查

| 页面 | 当前可用性 | 主要问题 | 已处理 | 后续方向 |
| --- | --- | --- | --- | --- |
| `/` 新手首页 | 可读取统计、任务和报告 | 原先像数据库仪表盘 | 改成三步入口和准备情况 | 增加示例标的一键填充 |
| `/backtests` 创建回测 | 可提交任务、取消、重试 | 参数太多，新手不知道哪些必填 | 拆成基础设置和高级参数 | 做成真正分步向导，自动推荐模板 |
| `/reports` 查看报告 | 可筛选和打开报告 | 列表仍偏技术字段 | 暂未改 | 增加“好/一般/风险高”的解释型摘要 |
| `/reports/[id]` 报告详情 | 可看曲线、摘要、交易、事件 | 指标名偏量化和英文 | 暂未改 | 增加结论卡片和指标解释 |
| `/market-data` 数据准备 | 可查看覆盖和同步 | 名称和字段偏数据库 | 导航改名为数据准备 | 增加“数据够不够跑回测”的检查结果 |
| `/templates` 策略模板 | 可新建编辑模板 | 对新手过于专业 | 导航归入策略模板 | 默认隐藏复杂参数，提供模板说明 |
| `/platform` 系统状态 | 可看服务、进程、日志 | 不应作为主入口 | 导航降级为维护入口 | 仅面向开发/运维说明用途 |

## 重构原则

- 首页只回答三个问题：能不能开始、怎么开始、结果在哪里看。
- 新手路径默认少填参数，复杂参数放到“高级”区域。
- 页面文案避免把数据库表、进程和内部模块作为主概念，除非用户进入系统状态页。
- 报告页优先解释结果含义，再展示底层指标。
