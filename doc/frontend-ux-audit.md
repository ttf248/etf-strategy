# 前端体验审查

本文记录面向“小白用户的回测平台”的前端页面审查结论。目标是把平台从运维控制台逐步调整为开箱即用的回测实验室。

## 总体判断

- 当前功能链路基本完整：数据准备、模板、提交回测、任务状态、报告列表和报告详情都有对应页面。
- 主要问题是信息架构偏运维：数据库、Worker、Scheduler、日志和模板管理过早暴露，用户还没完成第一次回测就需要理解平台内部概念。
- 第一轮调整已完成：导航改为“新手首页、创建回测、查看报告、数据准备、策略模板”，系统状态降级为维护入口；首页和回测页改为新手流程。
- 第二轮调整已完成：报告页增加解释型结论，数据准备页增加标的可回测检查，继续弱化内部表和进程概念。
- 第三轮调整已完成：策略模板页从参数后台改为模板库，高级编辑入口保留但不再作为新手主路径。
- 第四轮调整已完成：报告详情页继续翻译参数、交易和事件字段，不再把事件 JSON 作为默认阅读内容。
- 第五轮调整已完成：创建回测页增加策略选择卡片，用场景、风险和模板可用性替代单纯策略代码选择。
- 第六轮调整已完成：运行时逐页检查前端和关键 API，系统状态页在心跳表未初始化时给出 `init-db` 指引，后台进程不再持续刷 SQL 错误。
- 第七轮调整已完成：数据准备页的标的检查改为点击或回车触发，检查结果和覆盖表筛选同步更新。
- 第八轮调整已完成：截图检查发现移动端有 Ant Design Drawer 弃用告警导致开发浮层，已改用当前 API。
- 第九轮调整已完成：报告列表移动端改为卡片视图，保留结论、收益、回撤、交易次数和打开详情入口。
- 第十轮调整已完成：策略模板页移动端改为模板卡片，保留策略、周期、口径、状态、适用场景和操作；高级编辑 Drawer 同步改用当前 API，避免开发告警浮层。
- 第十一轮调整已完成：创建回测页移动端任务记录改为卡片，保留状态、进度、模板、错误说明和报告入口。
- 第十二轮调整已完成：数据准备页移动端覆盖记录改为卡片，保留周期、K 线数量、覆盖区间和最近更新。
- 第十三轮调整已完成：报告详情页移动端交易记录和事件流水改为卡片，常见报告字段转中文，减少横向表格和英文指标阅读成本。
- 第十四轮调整已完成：系统状态页改为维护检查页，默认显示健康摘要，进程、日志和调度折叠为高级维护信息。
- 第十五轮调整已完成：首页把同步和任务表折叠为高级数据明细，默认展示最近报告卡片和主路径入口。
- 第十六轮调整已完成：创建回测页从长表单改为三步向导，支持推荐模板选择和提交前确认。
- 第十七轮调整已完成：报告列表增加前端对比区，可选择多份报告对比收益、回撤和成交次数。
- 第十八轮调整已完成：数据准备页增加按当前标的一键同步入口，检查发现缺数据后可直接补数据。
- 第十九轮调整已完成：报告列表增加本地收藏、只看收藏筛选，并把收藏状态同步到移动端卡片、桌面对比区和表格操作列。
- 第二十轮调整已完成：数据准备页增加缺失周期推荐，直接告诉用户应补 1d、15m 还是 1m，并允许从推荐项一键同步。
- 第二十一轮调整已完成：首页增加“现成示例标的”，创建回测页支持从首页一键带入示例，并在进入下一步时自动套用推荐模板。
- 第二十二轮调整已完成：报告详情页增加“这几个数字怎么判断”解释区，把收益、回撤、买入持有对比、交易活跃度和下一步建议整理成阅读卡片。
- 第二十三轮调整已完成：首页增加“最近一次成功路径”，支持直接打开上次成功报告，或按相同标的、周期、策略和模板重回创建回测页。
- 第二十四轮调整已完成：模板页增加“推荐模板”区，并给推荐卡、移动端卡片和桌面表格都补上“去回测”入口，减少只会编辑不会使用的后台感。
- 第二十五轮调整已完成：报告详情页增加“回到报告列表”和“按当前配置重跑”入口，减少用户读完报告后还要手动回填配置。
- 第二十六轮调整已完成：系统状态页首屏进一步降级，增加“什么时候不用看这页”和按问题类型分流的维护路径卡，减少数据库、队列和进程信息的首屏干扰。
- 第二十七轮调整已完成：报告列表的对比区增加“对比后下一步”结论和直达动作，并给报告卡片、对比卡、桌面表格补上“按此配置重跑”入口。
- 第二十八轮调整已完成：补充 Playwright 主路径冒烟验证，默认复用本地 `3000` 前端或自动拉起测试实例，并用真实 API 验证首页到创建回测提交流程。
- 第二十九轮调整已完成：数据准备页增加新手示例标的推荐和当前库质量提示，把“先检查哪个标的、需不需要全量补数”直接讲清楚。
- 第三十轮调整已完成：报告详情页增加“去对比同标的报告”入口，报告列表支持通过 query 预带入当前报告和筛选条件，减少读完报告后再手工翻列表。
- 第三十一轮调整已完成：模板页增加“按目标找模板”和模板对比区，把“适合谁用、先试哪一个”直接说清楚。
- 第三十二轮调整已完成：首页增加“现在推荐你先做这一步”推荐卡，并给最近成功路径补上收益、回撤和成交情况摘要，减少用户回到首页后还要自己判断下一步。
- 第三十三轮调整已完成：报告详情页把“净值与回撤”区改成可读复盘区，新增曲线摘要、读图卡和图例说明，并把回撤线固定到 0% 下方。
- 第三十四轮调整已完成：全局壳层移除顶部数据库/时区/运行环境状态胶囊，改成基于当前页面的使用建议，并把侧栏分组改成“开始使用 / 排障时再看”。
- 第三十五轮调整已完成：报告列表桌面首屏从后台表格改成报告卡片库，对比空态也改成带动作的引导区，高级表格降级为折叠视图。
- 第三十六轮调整已完成：系统状态页首屏改成“要不要继续停留在这里”的判断页，并修正任务统计口径，把当前待处理任务和历史失败任务分开。
- 第三十七轮调整已完成：创建回测页把任务历史区改成“最近回测任务”摘要和最近任务卡片，完整历史与批量操作降级到高级折叠区。
- 第三十八轮调整已完成：数据准备页把完整覆盖表降级为高级明细折叠区，并把“当前数据准备提示”改成带动作的准备程度分层。
- 第三十九轮调整已完成：策略模板页把完整模板库和模板维护动作降级为高级管理折叠区，首屏优先服务“选模板去回测”。
- 第四十轮调整已完成：首页把推荐动作、示例标的和首页报告卡片的“为什么推荐”补成显式解释，不再只给动作入口。
- 第四十一轮调整已完成：报告列表把首屏排序依据显式化，默认按“收藏 -> 稳健正收益 -> 高波动正收益 -> 没成交 -> 反面对照”排序，并给每张卡片补上“为什么先看它”的解释。
- 第四十二轮调整已完成：报告详情页把参数字段墙改成“这套配置大概是什么意思”解释区，先翻译模板定位、交易节奏、仓位成本和重跑方向，再把完整参数降级到高级明细。
- 第四十三轮调整已完成：创建回测页把“第一次怎么跑通”和“提交后怎么看任务”补成显式引导，避免用户刚提交就掉回后台任务中心视角。
- 第四十四轮调整已完成：数据准备页把“现在最该做什么 / 什么时候根本不用同步全部”补成显式判断，并把全量补数从页头主动作降级到高级区。
- 第四十五轮调整已完成：策略模板页把推荐顺序和高级管理边界显式化，直接说明“为什么先看这些模板、什么时候才需要进高级管理”。
- 第四十六轮调整已完成：首页把三步主路径顺序和全局壳层统一为“数据准备 -> 创建回测 -> 查看报告”，并补上“什么时候才去系统状态”的显式边界。
- 第四十七轮调整已完成：系统状态页把 `Worker / Scheduler / 进程控制` 等术语进一步翻成“回测执行服务 / 定时同步服务 / 本机服务”，让维护页也尽量按排障场景表达，而不是回到运维平台口径。
- 第四十八轮调整已完成：首页 Hero 和数据准备页页头继续去技术术语，把“数据库 / Worker / 调度器 / 数据库表”进一步翻成“内部服务 / 行情 / 完整数据明细”这类新手更容易理解的说法。
- 第四十九轮调整已完成：创建回测页和报告列表页继续去“任务中心 / 结果墙”口径，把“排队、批量取消、批量勾选”翻成更接近日常操作的“等待开始、取消所选、多选比较”。
- 第五十轮调整已完成：策略模板页和报告详情页的 `Strategy Presets / Result Detail` 眉标统一改成中文，收掉主路径里最后一批显眼的英文页头残留。
- 第五十一轮调整已完成：创建回测页、策略模板页、报告详情页和首页高级摘要里的 `ID / bps / Bar / 执行口径 / 寻参任务数` 继续翻成“编号 / 万分比 / K 线数 / 成交假设 / 同时尝试的参数组数”。
- 第五十二轮调整已完成：模板页高级编辑和完整模板库继续收口研究字段，把 `模板键 / 并行数 / 样本外起点 / 参数空间` 等标签翻成更自然中文，并让模板列表里的成交假设统一显示“实盘口径 / 研究口径”。
- 第五十三轮调整已完成：报告详情页高级参数明细继续对齐模板页口径，把 `寻参范围 / 样本外比例 / 左侧行情处理 / 研究默认` 等字段和值统一翻成更自然中文。
- 第五十四轮调整已完成：创建回测页高级参数区、模板摘要和最近任务卡继续统一口径，把 `分钟线样本外比例 / 并行数 / 左侧处理 / 双口径 / 任务 #` 等残留研究或后台说法改成更自然中文。
- 第五十五轮调整已完成：系统状态页继续收口维护术语，把 `排队 / PID / 等待队列` 等残留后台词汇统一翻成“等待开始 / 进程编号 / 等待中的任务”。
- 第五十六轮调整已完成：首页“最近一次成功路径”卡继续统一编号口径，把 `任务 #...` 改成“任务编号 ...”。
- 第五十七轮调整已完成：首页高级运行区继续统一口径，把 `去报告列表查找 / 高级运行明细 / 成功 / 失败 / 任务` 收成“去查看所有报告 / 高级明细 / 已完成 / 未跑通 / 编号”。
- 第五十八轮调整已完成：报告列表页继续统一编号展示，把对比区、报告卡片和高级表格里的 `#报告编号` 收成“编号 ... / 报告编号”。

## 页面审查

| 页面 | 当前可用性 | 主要问题 | 已处理 | 后续方向 |
| --- | --- | --- | --- | --- |
| `/` 新手首页 | 可读取统计、任务、报告、示例入口、推荐起步动作和最近成功路径 | 已补主路径顺序统一、成功路径编号口径和高级运行区中文化，后续仍可继续细化更个性化的推荐逻辑 | 改成三步入口、准备情况和最近报告卡片；同步/任务表折叠到高级明细；增加可直接跳转的现成示例标的；增加最近成功报告和复跑入口；新增起步推荐卡和成功摘要卡；补上“为什么现在推荐这一步 / 为什么先看这些报告”的解释卡；把首页三步顺序和全局壳层统一成“数据准备 -> 创建回测 -> 查看报告”，并补上“什么时候才去系统状态”的边界卡；继续把最近成功路径卡和高级运行区里的后台式口径翻成自然中文 | 后续细化不同用户阶段的推荐排序理由 |
| `/backtests` 创建回测 | 可提交任务、取消、重试，并支持示例一键带入 | 已补主路径冒烟验证和高级区中文化，后续仍可继续扩展更多关键动作覆盖 | 改为三步向导；策略选择改为场景卡片；支持推荐模板；移动端任务记录改成卡片；支持从首页和页面内示例一键带入，并在下一步自动应用推荐模板；提交时改为读取整份保留表单，避免三步向导丢失前序字段；新增 Playwright 主路径冒烟验证；任务历史区改成最近任务摘要和最近 6 条任务卡片，完整历史与批量操作降级为高级折叠；首屏补上“第一次怎么跑通”和“提交后怎么看任务”的显式说明；继续把高级参数和任务卡里的研究字段翻成更自然中文 | 后续把报告页、数据准备页等关键动作继续纳入自动化 |
| `/reports` 查看报告 | 可筛选、对比、收藏、打开并直接重跑 | 收藏仍只保存在当前浏览器，如需跨设备同步还需要后端支持 | 增加简单结论、收益、回撤和解释文案；移动端改成报告卡片；增加前端报告对比；增加本地收藏和只看收藏筛选；增加对比后下一步结论和重跑入口；支持通过 query 预带入详情页选中的报告和筛选条件；桌面主视图改成卡片库并把高级表格折叠；首屏显式说明报告排序依据，并给每张卡片补上“为什么先看它”的解释；继续把“批量勾选”翻成更接近日常阅读动作的“多选比较”；继续把各区里的 `#报告编号` 翻成自然中文 | 如需跨设备同步，再补后端收藏能力；后续可继续细化同标的/同模板维度的排序解释 |
| `/reports/[id]` 报告详情 | 可看曲线、摘要、配置解释、交易、事件，并可直接重跑或带去对比 | 已补参数解释区和高级参数中文化，后续仍可继续补更细的参数影响解释和局部时段聚焦 | 增加顶部结论卡片；翻译策略、参数、交易和事件；移动端交易和事件改成卡片；补充收益、回撤、买入持有对比和下一步建议解释卡片；增加返回列表、按当前配置重跑和直达对比入口；把净值区改成可读复盘区并修正回撤坐标表达；把参数区改成先解释模板定位、交易节奏、仓位成本和重跑方向，再把完整参数降级到高级明细；继续把高级参数字段和值翻成更自然中文 | 后续继续补更细的参数影响提示和局部时段聚焦 |
| `/market-data` 数据准备 | 可查看覆盖、检查标的、按推荐周期同步，也能直接使用示例标的开始 | 已补示例标的推荐和全量补数降级，后续仍可继续细化更深的数据质量校验 | 增加标的可回测检查、当前标的一键同步、周期卡片和补救同步入口；移动端覆盖记录改成卡片；补充缺失周期推荐和一键同步；新增示例标的推荐与当前库质量提示；首屏增加“先补一个能用的标的”起步卡；完整覆盖表降级为高级明细折叠区；补上“现在最该做什么 / 什么时候才同步全部”的显式判断，并把全量补数移入高级区 | 后续补更细的数据完整性和停牌/覆盖异常提示 |
| `/templates` 策略模板 | 可新建编辑模板、按目标筛模板、对比后再去回测 | 已补推荐顺序、高级管理边界和高级参数中文化，后续仍可继续细化更深的策略解释 | 增加模板选择说明、策略适用场景和高级编辑提示；移动端改成模板卡片；增加推荐模板和直达回测入口；新增“按目标找模板”和模板对比区；首屏增加“不要一上来新建高级模板”起步卡；完整模板库降级为高级管理折叠区；补上“这些模板为什么先看”和“什么时候才需要进高级管理”的显式解释；继续把高级编辑里的研究字段翻成更自然中文 | 后续补更细的策略差异解释和参数影响提示 |
| `/platform` 系统状态 | 可做维护判断并在需要时展开服务心跳、定时同步、本机服务和日志 | 仍仅面向维护，不应进入新手主路径 | 导航降级为维护入口；默认只展示健康摘要；进程、日志、调度折叠到高级维护信息；首屏增加“什么时候不用看这页”和问题分流卡；把当前待处理任务与历史失败任务分开展示；把 `Worker / Scheduler / 进程 / PID / 排队` 继续翻译成排障场景语言 | 仅面向开发/运维说明用途 |

## 全局壳层审查

- 当前全局导航已从“控制台壳层”继续往“小白回测平台壳层”收敛：侧栏补了固定主路径提示卡，顶部不再展示数据库、时区和本地运行状态。
- 现在无论用户落在首页、报告页还是数据准备页，页头都会先告诉用户“这页最适合做什么”，而不是先暴露技术环境信息。
- 维护入口仍保留，但明确放到“排障时再看”分组，避免 `/platform` 和日常回测路径并列造成误导。

## 运行时检查记录

2026-05-27 本地检查：

- 前端路由 `/`、`/backtests`、`/reports`、`/market-data`、`/templates`、`/platform` 均返回 200。
- 后端接口 `/api/market-data/stats`、`/api/backtests?limit=5`、`/api/reports?limit=5`、`/api/templates?active_only=true`、`/api/platform/status` 均返回 200。
- 发现本地数据库缺少 `platform_heartbeats` 表时，Worker/Scheduler 会重复输出底层 SQL 错误；已改为一次性初始化提示。
- 使用 Playwright 对 6 个页面生成桌面和移动端截图，输出到 `outputs/frontend-screenshots/`；截图发现开发浮层 `1 Issue`，来源为 Drawer `width` 弃用告警，已修复。

2026-05-27 复核：

- 前端路由 `/`、`/backtests`、`/reports`、`/reports/2`、`/market-data`、`/templates`、`/platform` 均返回 200。
- 后端接口 `/api/market-data/stats`、`/api/backtests?limit=5`、`/api/reports?limit=5`、`/api/reports/2`、`/api/templates?active_only=true`、`/api/platform/status` 均返回 200。
- 使用 Playwright 重新生成 7 个移动端截图，输出到 `outputs/frontend-screenshots/final-mobile-pass/`；未再出现红色开发错误浮层。
- 当前移动端主路径已经覆盖：首页开始回测、创建回测、查看报告、报告详情、数据准备、策略模板和系统状态维护页。

2026-05-28 冒烟验证补充：

- 新增 `frontend/playwright.config.ts` 和 `frontend/tests/smoke/backtest-lab.spec.ts`。
- 通过 `npm run test:smoke` 默认复用本地 `3000` 前端或自动拉起测试实例，使用真实 FastAPI 接口验证“首页 -> 创建回测 -> 提交任务”的主路径。
- 冒烟用例会先检查样例行情和启用模板是否可用，再在提交成功后对新任务补发取消请求，减少测试对本地任务队列的残留影响。
- 冒烟验证过程中发现三步向导最终提交只带出了当前步骤挂载字段，已修正为提交整份保留表单，避免 `symbol`、`interval`、`template_id` 在最后一步丢失。

2026-05-28 数据准备页补充：

- 数据准备页新增“新手建议先用这些标的”，复用首页同一套示例逻辑，允许先检查再决定是否直接去回测。
- 页面新增“当前数据准备提示”，用“可直接首跑 / 只适合长周期 / 还需补关键周期”三类提示减少用户误以为必须先做全量建库。
- 当前页现在同时回答三个问题：先检查哪个标的、缺哪些周期、要不要继续同步全部数据。

2026-05-28 报告对比联通补充：

- 报告详情页新增“去对比同标的报告”入口和下一步卡片，读完当前结果后可以直接进入对比流程。
- 报告列表支持读取 `compare`、`keyword`、`interval` query 参数，自动带入当前报告和同标的筛选条件。
- 新增 Playwright 冒烟验证，覆盖“报告详情 -> 报告对比”这条联通路径。

2026-05-28 模板页补充：

- 模板页新增“按你的目标找模板”，支持按“第一次先跑一轮 / 想看长期持有 / 想试日线择时 / 已经会看分钟波动”快速筛模板。
- 模板页新增模板对比区，可直接比较“适合谁、难度、默认状态”，再决定先用哪一个去回测。
- 模板列表和推荐卡按更偏新手的规则排序，优先展示默认模板和更容易上手的组合。
- 新增 Playwright 冒烟验证，覆盖“模板页 -> 目标筛选 -> 加入对比”这条路径。

2026-05-28 首页补充：

- 首页新增“现在推荐你先做这一步”推荐卡，按最近成功报告、现成示例标的和当前数据准备情况决定首个动作，减少用户回到首页后重新判断路径。
- “最近一次成功路径”下方新增三张摘要卡，直接用样本外收益、最大回撤和成交次数解释结果值不值得继续看、波动大不大以及建议的下一步。
- 最近报告卡片补充一句简短判断，帮助用户先决定“打开细看”还是“拿去和别的结果对比”。

2026-05-28 报告详情页图表补充：

- 本地桌面和移动端截图复核发现，报告详情页虽然已有收益/回撤解释卡，但“净值与回撤”区仍然像原始图表控件，用户需要自己猜三条线分别代表什么。
- 该区域已新增曲线摘要横幅、三张读图卡和一条图例说明，直接回答“走势整体如何、最该看哪个时点、应该按什么顺序读图”。
- 净值图右侧刻度改为明确百分比，并把回撤线按负值绘制到 0% 下方，同时标出权益高点和最深回撤，降低把回撤误解为正向表现的风险。

2026-05-28 全局壳层补充：

- 桌面截图复核发现，虽然主导航已经换成“新手首页 / 创建回测 / 查看报告”，但顶部 `PostgreSQL / Asia/Shanghai / 本地运行` 胶囊仍然让所有页面保持明显运维平台气质。
- 这轮已把顶部状态胶囊改成当前页面的使用建议，并在左侧增加“第一次使用建议”卡片，固定提示“数据准备 -> 创建回测 -> 查看报告”的起步顺序。
- 移动端页头维持轻量，只保留菜单和当前页标题，不再暴露运维状态；维护入口继续保留，但降级到“排障时再看”分组。

2026-05-28 报告列表页补充：

- 桌面截图复核发现，报告列表虽然移动端已经卡片化，但桌面首屏仍然直接回退到 Ant 表格，和“面向小白用户的回测平台”目标不一致。
- 这轮已把桌面主视图改成和移动端一致的报告卡片库，先展示结论、收益、回撤、交易和下一步动作；高级表格仅在需要批量勾选和精细筛选时再展开。
- 报告对比空态补成带动作的引导区，明确提示“先从下面的报告卡片挑 2 到 4 份”，并提供打开收益最高或最近生成报告的快捷入口。

2026-05-28 系统状态页补充：

- 截图和运行时检查发现，系统状态页首屏虽然已经折叠了大量维护信息，但“后台任务 23 / 当前没有排队任务”这种累计口径仍然会误导用户，把历史状态误看成当前堆积。
- 这轮已把首屏摘要改成“现在需不需要处理 / 当前待处理任务 / 服务异常数 / 历史失败任务”，先解决“要不要排障”和“是不是当前卡住”两个核心判断。
- 高级折叠项标题改成场景句式，例如“如果页面打不开，再看心跳与同步调度”；并新增冒烟验证，确保维护页首屏优先展示判断结论而不是直接暴露原始运维指标。

2026-05-28 创建回测页任务区补充：

- 桌面和移动端截图复核发现，虽然创建回测页已经完成三步向导，但任务历史区仍然让首屏像后台任务中心，用户一进页就会先看到长列表和批量操作。
- 这轮已把该区域改成“最近回测任务”摘要横幅，先展示运行中、排队、失败和已完成数量，并提供“打开最近成功报告 / 去看报告列表”的快捷动作。
- 最近任务只默认展示 6 条卡片，每条都补上“现在该怎么看”的提示文案；完整历史任务表、批量取消和批量重试统一降级到高级折叠区。
- 冒烟用例同步新增“先看最近几次任务，再决定要不要展开完整历史”断言，确保后续不会回退成默认长表格首屏。

2026-05-28 数据准备页补充：

- 继续复核发现，数据准备页虽然已经有标的检查卡、示例标的和质量提示，但首屏下半段仍然直接暴露完整覆盖表，用户很容易回到“先翻数据库里都有什么”的后台视角。
- 这轮新增“先补一个能用的标的，再决定要不要同步全部数据”起步卡，按当前检查结果直接给出“去回测 / 先补推荐周期 / 先用现成示例标的开始”的动作。
- “当前数据准备提示”改成带动作的准备程度分层卡：分别展示“可直接首跑 / 只适合长周期 / 还需补关键周期”，并给每类附上可直接检查的示例标的按钮。
- 完整覆盖表降级为“高级明细：全部标的覆盖、筛选和更新时间”折叠区，首屏先说明什么时候根本不需要展开它；同时新增 smoke 验证，确保数据准备页主路径优先展示开始建议而不是默认大表格。

2026-05-28 策略模板页补充：

- 继续复核发现，模板页虽然已经有推荐模板和对比区，但首屏仍然直接暴露“新建高级模板”和完整模板库，容易把新手带回参数后台，而不是先选模板去回测。
- 这轮新增“先从推荐模板里挑一个开始，不要一上来新建高级模板”起步卡，把默认动作固定为“先用默认模板去回测”或“只看第一次先跑一轮”。
- 完整模板库、启用停用和新建模板统一降级为“高级模板管理”折叠区，首屏先明确这些能力只在需要维护模板时再展开。
- 模板页冒烟验证同步新增“高级管理：启用停用、新建模板和完整模板库”断言，确保后续不会回退成首屏直接暴露模板后台。

2026-05-28 首页补充：

- 继续复核发现，首页虽然已经有“现在推荐你先做这一步”、示例标的和最近报告卡片，但推荐逻辑更多停留在系统替用户做了决定，还没有把“为什么是这一步、为什么是这些卡片”明确说出来。
- 这轮给起步推荐卡补上两张归因卡，直接解释为什么现在先走这条路径、为什么不是先去别的页面。
- “现成示例标的”每张卡新增“为什么优先推荐它 / 更适合怎么用”解释块，不再只靠一句总括文案。
- 首页报告区改成“现在更适合先看的报告”，并给每张卡片补上“为什么现在先看它”的说明，区分“刚跑通的成功样本 / 适合先看 / 重点看波动 / 适合做反面对照”。
- 首页冒烟验证同步新增“为什么现在推荐这一步”断言，确保首页后续不会退回成只有按钮入口、没有推荐依据的薄引导页。

2026-05-28 报告列表排序补充：

- 继续复核发现，报告列表虽然已经改成卡片库，但“为什么这些报告排在前面”仍然是隐藏规则，新手只能看到结果顺序，无法理解系统在优先展示什么。
- 这轮已把首屏横幅改成显式排序说明，直接告诉用户当前列表按“收藏 -> 稳健正收益 -> 高波动正收益 -> 没成交 -> 反面对照”排序，而不是按时间简单堆叠。
- 每张报告卡新增“为什么先看它”的解释块，和真实排序逻辑共用同一套判断，避免出现文案解释和实际顺序不一致的情况。
- 高级表格视图也改为沿用同一排序口径，并新增“为什么先看”列；Playwright 冒烟同步更新，防止后续回退成只有卡片没有排序解释的结果墙。

2026-05-28 报告详情参数区补充：

- 继续复核发现，报告详情页虽然图表和结果解释已经可读，但“本次使用的关键参数”仍然是一整块字段墙，新手很难判断哪些参数值得先看、哪些暂时可以忽略。
- 这轮已把该区域改成“这套配置大概是什么意思”，默认先展示四张解释卡：模板定位、交易节奏、仓位与成本、如果要重跑先改什么。
- 完整参数字段和模板追踪仍然保留，但统一降级到“高级参数明细：全部字段与模板追踪”折叠区，避免首次阅读时直接掉回研究后台视角。
- 报告详情的 Playwright 冒烟同步新增配置解释区断言，确保后续不会回退成只有参数字段、没有新手解释的详情页。

2026-05-28 创建回测页提交引导补充：

- 继续复核发现，创建回测页虽然已经有三步向导和最近任务卡片，但仍然默认假设用户知道“第一次应该直接怎么选”“提交之后到底看哪里”，新手仍然容易被任务区拉回后台心智。
- 这轮在向导顶部新增“如果你只是想先跑通第一轮”横幅，直接把建议路径固定成“选示例标的 -> 用推荐模板 -> 提交后看最近任务第一条”。
- 策略步骤新增推荐模板解释横幅，把“为什么先用这个模板”说清楚，而不是只给一个按钮。
- 提交步骤新增三张说明卡，分别解释“提交后会发生什么”“什么时候看任务区”“什么时候其实不用继续停留在这页”；最近任务横幅同步补上“刚提交成功时，只看第一条就够了”的提醒。
- Playwright 主路径冒烟同步新增这些引导文案断言，防止后续回退成“能提任务，但不会读任务”的后台型创建页。

2026-05-28 数据准备页决策补充：

- 继续复核发现，数据准备页虽然已经有“先补一个能用的标的”起步卡，但页头仍然直接暴露“同步全部”，而且页面还缺一句更明确的判断，直接告诉用户“现在该补当前标的，还是根本不用继续补全量”。
- 这轮给起步卡补上三张判断卡：当前判断、现在最该做、什么时候才同步全部，把“先补当前标的 / 直接去回测 / 暂时不用全量补数”明确说清楚。
- “同步全部”从页头主动作降级到“高级补数”区，只在需要扩大量级、补更多标的时再使用，避免新手一进页面就误以为必须先做全量建库。
- 数据准备页 smoke 同步新增“什么时候才同步全部”和“高级补数”断言，确保后续不会回退成默认暴露全量补数的后台式数据页。

2026-05-28 策略模板页边界补充：

- 继续复核发现，模板页虽然已经把完整模板库折叠起来，但推荐区仍然缺两层关键解释：为什么这些模板排在前面，以及到底什么时候才需要进入高级管理。
- 这轮在推荐模板区新增排序说明横幅，直接告诉用户当前会先展示默认模板、更容易上手的长期或基础模板，最后才是分钟进阶和专项模板。
- 每张推荐模板卡新增“为什么先看它”的解释块，把排序逻辑翻译成用户可理解的理由，而不是只靠排序结果暗示。
- 高级模板管理区新增三张边界卡，分别说明“什么时候要进这里”“什么时候值得编辑”“什么时候其实不用碰”，把模板后台能力进一步从新手主路径中隔离出来。
- 模板页 smoke 同步新增推荐顺序说明和高级管理边界断言，避免后续回退成“只有推荐卡和后台库，但没有解释”的模板中心页面。

2026-05-28 首页与壳层顺序补充：

- 继续复核发现，虽然全局壳层、侧栏提示和多数页面都已经统一成“数据准备 -> 创建回测 -> 查看报告”，但首页三步卡仍然在用“创建回测 -> 阅读结果 -> 准备数据”的旧顺序，会直接把用户往错误节奏上带。
- 这轮已把首页三步卡顺序统一回“数据准备 -> 创建回测 -> 查看报告”，和左侧“第一次使用建议”以及各页面页头提示保持一致。
- 首页同步新增“只有页面打不开、任务长期不动或连续失败时，再去系统状态”的边界卡，把系统状态明确降级为排障入口，而不是主路径第四步。
- 首页 smoke 同步新增该边界断言，确保后续不会重新出现首页主路径和全局壳层口径不一致的问题。

2026-05-28 系统状态页术语收口补充：

- 继续复核发现，`/platform` 虽然已经降级为维护页，但折叠标题、日志筛选和服务名里仍直接暴露 `Worker / Scheduler / 进程`，对小白用户来说依然像运维平台。
- 这轮把日志服务筛选、心跳表和本机服务表里的服务名统一翻译成“接口服务 / 回测执行服务 / 定时同步服务”，并把“进程控制”改成“服务重启仅供维护使用”。
- 折叠区标题同步改成“如果页面打不开，再看服务心跳与定时同步”“如果怀疑执行服务没运行，再看本机服务”，优先告诉用户为什么要展开，而不是先让用户理解底层组件名。
- 系统状态页 smoke 同步新增新术语断言，避免后续回退成直接暴露英文服务名和进程术语的维护页面。
- 同时修正首页和数据准备页 smoke 的等待方式：先等页面自身请求的 API 返回，再断言首屏文案，避免首屏还停留在 Skeleton 时误判为页面缺少标题。

2026-05-28 首页与数据准备页术语补充：

- 继续复核发现，虽然系统状态页已经基本去掉运维平台口径，但首页 Hero 还直接写着 `数据库、Worker、调度器`，数据准备页页头和空态也仍然提 `数据库表` 和“当前数据库没有这个标的”。
- 这轮把首页 Hero 文案改成“不用先理解内部服务怎么运作”，保留“先跑通回测主路径”的核心意思，但不再要求新手理解内部组件名。
- 数据准备页页头同步改成“缺数据时再同步，不需要先研究完整数据明细”，并把“数据库里没有这个标的”统一翻成“当前还没有这个标的的行情”。
- 这轮没有删除任何数据检查、同步和高级明细能力，只是继续把首屏主叙事从“系统里怎么存数据”翻成“现在能不能直接开始回测”。

2026-05-28 创建回测页与报告列表页术语补充：

- 继续复核发现，`/backtests` 和 `/reports` 的主路径已经正确，但“排队 / 批量取消 / 批量勾选 / 结果列表”这类说法，仍然会把页面气质拉回任务中心和后台表格。
- 这轮把创建回测页摘要里的“排队”翻成“等待开始”，把“批量取消 / 批量重试”改成“取消所选 / 重试所选”，让高级区更像例外处理，而不是首页主动作。
- 报告列表页把主卡标题改成“先挑几份值得细看的报告”，高级表格区则改成“多选比较与精细筛选”，把阅读和比较放在“批量勾选”之前。
- 这轮没有删除任何多选处理、完整历史或高级表格能力，只是继续把默认叙事从“管理很多任务/结果”改成“先挑一两份值得看、再决定要不要深入处理”。

2026-05-28 跨页英文页头补充：

- 继续复核发现，虽然首页、数据准备、创建回测、查看报告的主文案已经大体中文化，但策略模板页和报告详情页的眉标仍然保留 `Strategy Presets`、`Result Detail`，在整套新手路径里显得突兀。
- 这轮只收口这两个共享页头残留，统一改成“策略模板”“报告详情”，让主路径页面的页头层级不再混用中英文。
- 这轮没有改页面结构、交互或正文解释，只处理跨页一致性最显眼的英文残留，避免把本轮边界重新扩大。

2026-05-28 常见专业缩写补充：

- 继续总扫发现，主路径里的大块英文已经基本清掉，但创建回测页、模板页和报告详情页仍然直接暴露 `ID / bps / Bar / 执行口径 / 寻参任务数` 这类研究或开发缩写。
- 这轮把这些高频词统一翻成“编号 / 万分比 / K 线数 / 成交假设 / 同时尝试的参数组数”，优先降低第一次阅读时的理解门槛。
- 同步把首页高级数据明细里的 `ID` 改成“编号”，并把创建回测页成功提示里的 `ID=...` 改成“编号=...”，让常见反馈信息也维持同一套中文口径。
- 这轮没有删除任何高级参数或追踪字段，只是把最常见的技术缩写翻译成更像自然语言的说明，保留研究能力但减少术语负担。

2026-05-28 模板页高级字段补充：

- 继续总扫发现，模板页首屏已经不再像参数后台，但高级编辑抽屉和完整模板库里仍保留 `模板键 / 并行数 / 样本外起点 / 参数空间` 等研究型字段，用户一进高级区仍然需要自己翻译含义。
- 这轮把这些字段统一翻成更自然的说明，例如“模板标识”“同时尝试的参数组数”“从哪一天开始算样本外”“可尝试的参数范围”，并把模板列表里的成交假设统一显示为“实盘口径 / 研究口径”。
- 这轮没有改模板参数结构或接口字段名，只收口前端展示标签，保证模板服务和历史数据兼容。

2026-05-28 报告详情高级参数补充：

- 继续总扫发现，报告详情页虽然已经把参数区改成解释卡，但高级参数明细里仍保留 `寻参范围 / 样本外比例 / 样本外起点 / 左侧行情处理 / 研究默认` 这类偏研究口径的字段和值，和模板页新的中文说明也不一致。
- 这轮把这些字段和值继续对齐成“可尝试的参数范围”“留给样本外验证的比例”“从哪一天开始算样本外”“左侧行情时怎么处理”“研究口径”，减少跨页切换时的理解成本。
- 这轮没有改报告文件内容、回测指标或模板快照结构，只收口详情页前端展示映射。

2026-05-28 创建回测页高级区补充：

- 继续总扫发现，创建回测页主路径已经正确，但高级参数区、模板摘要和最近任务卡里仍保留 `分钟线样本外比例 / 并行数 / 左侧处理 / 双口径 / 任务 #` 这类研究或后台口径。
- 这轮把这些字段统一翻成“留给样本外验证的比例”“同时尝试的参数组数”“左侧行情时怎么处理”“两种都保留”“任务编号”，并让模板摘要里的成交假设继续显示成“实盘口径 / 研究口径”。
- 这轮没有改回测提交、取消、重试或模板带入逻辑，只继续收口创建回测页的前端展示语言。

2026-05-28 系统状态页维护术语补充：

- 继续总扫发现，系统状态页虽然已经降级成维护页，但任务摘要、路径提示和两张维护表里仍保留 `排队 / PID / 等待队列` 这类显眼后台词汇。
- 这轮把它们继续统一翻成“等待开始”“进程编号”“等待中的任务”，让维护页即使保留排障能力，也尽量先围绕“现在要看什么问题”表达。
- 这轮没有改平台状态接口、心跳数据或服务重启逻辑，只继续收口前端文案和表头。

2026-05-28 首页成功路径编号补充：

- 继续总扫发现，首页大块主文案已经基本去后台化，但“最近一次成功路径”卡里还保留 `任务 #...` 这类单点后台式展示。
- 这轮把它统一成“任务编号 ...”，让首页、创建回测页和报告详情页对编号的展示继续保持一致。
- 这轮没有改首页推荐逻辑、报告排序或高级运行明细，只处理首页这一处小残留。

2026-05-28 首页高级运行区补充：

- 继续总扫发现，首页主路径已经较清晰，但高级运行区仍保留 `去报告列表查找 / 高级运行明细 / 成功 / 失败 / 任务` 这类偏后台的表述。
- 这轮把它们继续翻成“去查看所有报告”“高级明细”“已完成”“未跑通”“编号”，让首页高级区也更像复盘记录，而不是后台任务面板。
- 这轮没有改首页统计逻辑、推荐排序或高级区表结构，只继续收口首页展示文案。

2026-05-28 报告列表编号展示补充：

- 继续总扫发现，报告列表页主结构已经偏向结果阅读，但对比区、卡片标题和高级表格里仍直接暴露 `#报告编号`，会把结果库气质拉回后台列表。
- 这轮把这些展示统一成“编号 ... / 报告编号”，让报告列表页更像可读的结果库，而不是带 `#ID` 的管理表。
- 这轮没有改排序规则、对比逻辑、收藏能力或报告详情跳转，只继续收口报告列表页前端展示文案。

## 重构原则

- 首页只回答三个问题：能不能开始、怎么开始、结果在哪里看。
- 新手路径默认少填参数，复杂参数放到“高级”区域。
- 页面文案避免把数据库表、进程和内部模块作为主概念，除非用户进入系统状态页。
- 报告页优先解释结果含义，再展示底层指标。
- 数据页优先回答“这个标的能不能跑”，覆盖表只作为排查细节。
